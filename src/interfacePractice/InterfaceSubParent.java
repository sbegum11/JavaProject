package interfacePractice;
//This is an interface.It is not a class.
// Interface is 100% abstraction because we cannot keep any non-abstract(instance method) method
// All the methods you create in Interface is by default abstract, so you don't need to use abstract word
// Any variable you include into Interface , by default it becomes final
public interface InterfaceSubParent {
    public float valueofPI= 3.15f;

    public void paycard();

    public void amountreduced();






}
